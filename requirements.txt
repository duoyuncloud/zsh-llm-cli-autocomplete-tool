# Core dependencies (daemon + plugin)
requests>=2.28.0
pyyaml>=6.0
argcomplete>=2.0.0
python-dotenv>=0.19.0
prompt-toolkit>=3.0.0
huggingface_hub>=0.16.0
gguf>=0.1.0

# Training dependencies: pip install -r requirements-training.txt