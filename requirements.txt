# Core runtime dependencies (daemon + plugin)
torch>=2.1.0
transformers>=4.40.0
peft>=0.10.0
sentencepiece>=0.1.99
huggingface_hub>=0.16.0

# Optional: Claude API for high-quality complex completions
# Install: pip install anthropic
# Then: export ANTHROPIC_API_KEY=sk-ant-...
# anthropic>=0.40.0

# Training dependencies: pip install -r requirements-training.txt